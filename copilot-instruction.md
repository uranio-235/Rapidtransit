# Copilot Instruction

For this repository, read `AGENT.md` before planning or executing a release.

If the user asks for a release, follow the release workflow and versioning rules defined in `AGENT.md`.
